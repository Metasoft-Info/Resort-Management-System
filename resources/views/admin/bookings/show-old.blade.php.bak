@extends('layouts.admin')
@section('content')
<div class="p-6">
    <div class="mb-6 flex justify-between items-center">
        <div>
            <h1 class="text-3xl font-bold text-gray-800">Booking Details #{{ str_pad($booking->id, 5, '0', STR_PAD_LEFT) }}</h1>
            <p class="text-gray-600 mt-2">Complete booking information and invoice</p>
        </div>
        <div class="flex gap-3">
            <a href="{{ route('admin.bookings.edit', $booking) }}" class="bg-blue-600 text-white px-6 py-3 rounded-lg hover:bg-blue-700 transition">
                <i class="fas fa-edit mr-2"></i>Edit Booking
            </a>
            <button onclick="window.print()" class="bg-green-600 text-white px-6 py-3 rounded-lg hover:bg-green-700 transition">
                <i class="fas fa-print mr-2"></i>Print Invoice
            </button>
            <a href="{{ route('admin.bookings.index') }}" class="bg-gray-500 text-white px-6 py-3 rounded-lg hover:bg-gray-600 transition">
                <i class="fas fa-arrow-left mr-2"></i>Back
            </a>
        </div>
    </div>

    <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
        <!-- Main Booking Info -->
        <div class="lg:col-span-2 space-y-6">
            <!-- Customer Information -->
            <div class="bg-white rounded-xl shadow-lg p-6">
                <h2 class="text-2xl font-bold text-gray-800 mb-6 flex items-center">
                    <i class="fas fa-user text-blue-600 mr-3"></i>
                    Customer Information
                </h2>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                    <div>
                        <label class="text-sm font-semibold text-gray-500">Name</label>
                        <p class="text-lg text-gray-800">{{ $booking->customer_name }}</p>
                    </div>
                    <div>
                        <label class="text-sm font-semibold text-gray-500">NID Number</label>
                        <p class="text-lg text-gray-800">{{ $booking->customer_nid }}</p>
                    </div>
                    <div>
                        <label class="text-sm font-semibold text-gray-500">Phone</label>
                        <p class="text-lg text-gray-800">{{ $booking->customer_phone }}</p>
                    </div>
                    <div>
                        <label class="text-sm font-semibold text-gray-500">WhatsApp</label>
                        <p class="text-lg text-gray-800">{{ $booking->customer_whatsapp ?? 'N/A' }}</p>
                    </div>
                    <div>
                        <label class="text-sm font-semibold text-gray-500">Email</label>
                        <p class="text-lg text-gray-800">{{ $booking->customer_email }}</p>
                    </div>
                    <div>
                        <label class="text-sm font-semibold text-gray-500">Passport Number</label>
                        <p class="text-lg text-gray-800">{{ $booking->passport_number ?? 'N/A' }}</p>
                    </div>
                    <div class="md:col-span-2">
                        <label class="text-sm font-semibold text-gray-500">Address</label>
                        <p class="text-lg text-gray-800">{{ $booking->customer_address ?? 'N/A' }}</p>
                    </div>
                </div>

                @if($booking->reference_name || $booking->reference_phone)
                <div class="mt-6 pt-6 border-t">
                    <h3 class="text-lg font-bold text-gray-700 mb-4">Reference Person</h3>
                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label class="text-sm font-semibold text-gray-500">Name</label>
                            <p class="text-lg text-gray-800">{{ $booking->reference_name ?? 'N/A' }}</p>
                        </div>
                        <div>
                            <label class="text-sm font-semibold text-gray-500">Phone</label>
                            <p class="text-lg text-gray-800">{{ $booking->reference_phone ?? 'N/A' }}</p>
                        </div>
                    </div>
                </div>
                @endif
            </div>

            <!-- Booking Details -->
            <div class="bg-white rounded-xl shadow-lg p-6">
                <h2 class="text-2xl font-bold text-gray-800 mb-6 flex items-center">
                    <i class="fas fa-bed text-green-600 mr-3"></i>
                    Booking Details
                </h2>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                    <div>
                        <label class="text-sm font-semibold text-gray-500">Room Number</label>
                        <p class="text-lg text-gray-800">{{ $booking->room->room_number }}</p>
                    </div>
                    <div>
                        <label class="text-sm font-semibold text-gray-500">Room Type</label>
                        <p class="text-lg text-gray-800">{{ $booking->room->roomType->name ?? 'N/A' }}</p>
                    </div>
                    <div>
                        <label class="text-sm font-semibold text-gray-500">Check-in</label>
                        <p class="text-lg text-gray-800">{{ $booking->check_in_date->format('d M Y') }} at {{ $booking->check_in_time }}</p>
                    </div>
                    <div>
                        <label class="text-sm font-semibold text-gray-500">Check-out</label>
                        <p class="text-lg text-gray-800">{{ $booking->check_out_date->format('d M Y') }} at {{ $booking->check_out_time }}</p>
                    </div>
                    <div>
                        <label class="text-sm font-semibold text-gray-500">Number of Guests</label>
                        <p class="text-lg text-gray-800">{{ $booking->number_of_guests }}</p>
                    </div>
                    <div>
                        <label class="text-sm font-semibold text-gray-500">AC Preference</label>
                        <p class="text-lg text-gray-800">{{ strtoupper($booking->ac_preference) }}</p>
                    </div>
                    <div>
                        <label class="text-sm font-semibold text-gray-500">Status</label>
                        <span class="px-3 py-1 rounded-full text-sm font-semibold
                            {{ $booking->status === 'confirmed' ? 'bg-green-100 text-green-700' : '' }}
                            {{ $booking->status === 'pending' ? 'bg-yellow-100 text-yellow-700' : '' }}
                            {{ $booking->status === 'cancelled' ? 'bg-red-100 text-red-700' : '' }}">
                            {{ ucfirst($booking->status) }}
                        </span>
                    </div>
                    <div>
                        <label class="text-sm font-semibold text-gray-500">Created By</label>
                        <p class="text-lg text-gray-800">{{ $booking->createdBy->name ?? 'N/A' }}</p>
                    </div>
                </div>
                @if($booking->notes)
                <div class="mt-6 pt-6 border-t">
                    <label class="text-sm font-semibold text-gray-500">Notes / Special Requests</label>
                    <p class="text-lg text-gray-800 mt-2">{{ $booking->notes }}</p>
                </div>
                @endif
            </div>
        </div>

        <!-- Payment Summary -->
        <div class="space-y-6">
            <div class="bg-gradient-to-br from-blue-50 to-indigo-50 rounded-xl shadow-lg p-6">
                <h2 class="text-2xl font-bold text-gray-800 mb-6 flex items-center">
                    <i class="fas fa-money-bill-wave text-emerald-600 mr-3"></i>
                    Payment Summary
                </h2>
                
                <div class="space-y-4">
                    <div class="flex justify-between items-center pb-3 border-b">
                        <span class="text-gray-600">Base Amount</span>
                        <span class="text-lg font-semibold">৳{{ number_format($booking->total_amount - $booking->vat_amount + $booking->discount_amount - $booking->extra_charges, 2) }}</span>
                    </div>

                    @if($booking->vat_enabled && $booking->vat_amount > 0)
                    <div class="flex justify-between items-center pb-3 border-b">
                        <span class="text-gray-600">VAT (15%)</span>
                        <span class="text-lg font-semibold text-blue-600">৳{{ number_format($booking->vat_amount, 2) }}</span>
                    </div>
                    @endif

                    @if($booking->discount_amount > 0)
                    <div class="flex justify-between items-center pb-3 border-b">
                        <span class="text-gray-600">Discount 
                            @if($booking->discount_type === 'percentage')
                            ({{ $booking->discount_percentage }}%)
                            @endif
                        </span>
                        <span class="text-lg font-semibold text-red-600">-৳{{ number_format($booking->discount_amount, 2) }}</span>
                    </div>
                    @endif

                    @if($booking->extra_charges > 0)
                    <div class="flex justify-between items-center pb-3 border-b">
                        <span class="text-gray-600">Extra Charges</span>
                        <span class="text-lg font-semibold text-orange-600">৳{{ number_format($booking->extra_charges, 2) }}</span>
                    </div>
                    @if($booking->extra_charges_description)
                    <p class="text-xs text-gray-500 -mt-2">{{ $booking->extra_charges_description }}</p>
                    @endif
                    @endif

                    <div class="flex justify-between items-center py-4 bg-white rounded-lg px-4">
                        <span class="text-xl font-bold text-gray-800">Total Amount</span>
                        <span class="text-2xl font-bold text-green-600">৳{{ number_format($booking->total_amount, 2) }}</span>
                    </div>

                    <div class="flex justify-between items-center py-3 bg-blue-100 rounded-lg px-4">
                        <span class="font-semibold text-gray-700">Advance Payment</span>
                        <span class="text-lg font-bold text-blue-600">৳{{ number_format($booking->advance_payment, 2) }}</span>
                    </div>

                    <div class="flex justify-between items-center py-3 bg-yellow-100 rounded-lg px-4">
                        <span class="font-semibold text-gray-700">Remaining</span>
                        <span class="text-lg font-bold text-yellow-600">৳{{ number_format($booking->remaining_payment, 2) }}</span>
                    </div>

                    <div class="pt-4 border-t">
                        <div class="flex justify-between items-center">
                            <span class="text-gray-600">Payment Method</span>
                            <span class="px-3 py-1 bg-gray-200 rounded-full text-sm font-semibold">{{ strtoupper($booking->payment_method) }}</span>
                        </div>
                        <div class="flex justify-between items-center mt-3">
                            <span class="text-gray-600">Payment Status</span>
                            <span class="px-3 py-1 rounded-full text-sm font-semibold
                                {{ $booking->payment_status === 'paid' ? 'bg-green-100 text-green-700' : '' }}
                                {{ $booking->payment_status === 'partial' ? 'bg-yellow-100 text-yellow-700' : '' }}
                                {{ $booking->payment_status === 'pending' ? 'bg-red-100 text-red-700' : '' }}">
                                {{ ucfirst($booking->payment_status) }}
                            </span>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Documents -->
            @if($booking->customer_photo || $booking->customer_nid_document || $booking->passport_document || $booking->visiting_card)
            <div class="bg-white rounded-xl shadow-lg p-6">
                <h3 class="text-xl font-bold text-gray-800 mb-4 flex items-center">
                    <i class="fas fa-file-image text-purple-600 mr-2"></i>
                    Documents
                </h3>
                <div class="space-y-3">
                    @if($booking->customer_photo)
                    <a href="{{ Storage::url($booking->customer_photo) }}" target="_blank" class="flex items-center text-blue-600 hover:text-blue-700">
                        <i class="fas fa-image mr-2"></i>Customer Photo
                    </a>
                    @endif
                    @if($booking->customer_nid_document)
                    <a href="{{ Storage::url($booking->customer_nid_document) }}" target="_blank" class="flex items-center text-blue-600 hover:text-blue-700">
                        <i class="fas fa-file mr-2"></i>NID Document
                    </a>
                    @endif
                    @if($booking->passport_document)
                    <a href="{{ Storage::url($booking->passport_document) }}" target="_blank" class="flex items-center text-blue-600 hover:text-blue-700">
                        <i class="fas fa-file mr-2"></i>Passport Document
                    </a>
                    @endif
                    @if($booking->visiting_card)
                    <a href="{{ Storage::url($booking->visiting_card) }}" target="_blank" class="flex items-center text-blue-600 hover:text-blue-700">
                        <i class="fas fa-image mr-2"></i>Visiting Card
                    </a>
                    @endif
                </div>
            </div>
            @endif
        </div>
    </div>
</div>
@endsection
